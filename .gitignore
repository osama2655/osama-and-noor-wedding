# Environment / credentials
.env
.env.*
*.local
config.local.php
config.php
db.local.php
secrets.php

# OS / editor cruft
.DS_Store

# Dependencies (none currently, but keep ignored)
node_modules/

# server-side DB credentials (never commit)
.db-secret.php
